import React from 'react';

const Challenges = () => {
  return (
    <div>
      <h1>Challenges & Open Issues</h1>
      <p>Key challenges in DNS over HTTPS (DoH) include:</p>
      <ul>
        <li>Performance overheads due to encryption.</li>
        <li>Compatibility issues with traditional DNS systems.</li>
        <li>Complexity in widespread deployment, especially in large-scale networks.</li>
      </ul>
    </div>
  );
};

export default Challenges;
